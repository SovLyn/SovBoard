{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "quick-selector"],
  "permissions": [
    "core:default",
    "opener:default",
    "store:default",
    "clipboard-x:default",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister"
  ]
}
